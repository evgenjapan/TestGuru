<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
<div class="single-question">
  <p>Вопрос #<%= @question.id %>: <%= @question.body %></p>
  <div class="answers">
    <p>Ответы:</p>
    <% @question.answers.each do |answer| %>
      <p><%= answer.body %></p>
    <% end %>
  </div>
</div>
</body>
</html>
