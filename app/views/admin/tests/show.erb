<div class="test-detail">
  <h1><%= @test.title %>, level: <%= @test.level %></h1>
  <p>Category: <%= @test.category.title %></p>
  <div class="test-question">
    <h3>Questions:</h3>
    <% @test.questions.each_with_index do |question, index| %>
      <div class="single-question">
        <span class="index-question"><%= index + 1 %></span>
        <p>Вопрос: <%= question.body %></p>
        <%= link_to 'Show detail', admin_question_path(question) %>
      </div>
    <% end %>
  </div>
  <%= link_to 'New question', new_admin_test_question_path(@test) %>
  <%= link_to 'Edit test', edit_admin_test_path %>
  <%= link_to 'Back', admin_tests_path %>
  <%= link_to 'Destroy', admin_test_path, method: :delete, data: {confirm: 'Are you sure?'} %>
</div>
